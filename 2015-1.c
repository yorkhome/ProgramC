#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
/*
#define N 10
//输入一行字符，统计其中英文单词的个数，单词之间以空格为间隔
int main() {
	char str[N], * p; int sum = 0;
	printf("请输入字符串：\n");
	gets(str);
	p = str;
	for(int i=1;i<=N;i++){
		if (*p >= 'a' && *p <= 'z' || *p >= 'A' && *p <= 'Z') {
			if (*(p + 1) >= 'a' && *(p + 1) <= 'z' || *(p + 1) >= 'A' && *(p + 1) <= 'Z') {

			}
			else {
				sum++;
			}
		}
		p++;
	}
	printf("sum=%d", sum);
}
*/