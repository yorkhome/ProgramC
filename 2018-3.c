#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
//
/*
int main() {
	char str1[10], str2[10] = {NULL}, * p; int j = 0;
	printf("ÇëÊäÈë£º");
	gets(str1);
	p = str1;
	while(*p!='\0') {
		if (*p >= '0' && *p <= '9') {
			
		}
		else {
			str2[j++] = *p;
			//strcpy(str2[j], *p);
		}
		p++;
	}
	printf("%s", str2);
}
*/