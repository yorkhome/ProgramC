#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
/*
#define N 4
#define M 4
//

int main() {
	int fun(int(*p)[N]);
	int a[M][N];
	printf("«Î ‰»Î£∫\n");
	for (int i = 0; i < M; i++) {
		for (int j = 0; j < N; j++) {
			printf("«Î ‰»Îa[%d][%d]:",i,j);
			scanf("%d", &a[i][j]);
		}
	}
	fun(a);
}
int fun(int (*p)[N]) {
	int sum = 0;
	for (int i = 0; i < M; i++) {
		for (int j = 0; j < N; j++) {
			if (i==0 || i == M - 1 || j == 0 || j == N - 1) {
				sum += p[i][j];
				//continue;
			}
		}
	}
	
	printf("sum=%d", sum);
	return sum;
}
*/