#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
/*
#define N 3

struct student {
	int number;
	int math;
	int c;
}stu[N];
int main() {
	int max=0,sum=0;
	printf("请输入：\n");
	for (int i = 0; i < N; i++) {
		scanf("%d %d %d", &stu[i].number, &stu[i].math, &stu[i].c);
	}
	//找最大值
	for (int i = 0; i < N; i++) {
		if (stu[i].c > max) {
			max = stu[i].c;
		}
	}
	//输出与最大值一样的学生信息
	printf("c语言最好的学生有：\n");
	for (int i = 0; i < N; i++) {
		if (stu[i].c == max) {
			printf("学号：%d,数学：%d,c语言：%d\n", stu[i].number, stu[i].math, stu[i].c);
			sum++;
		}
	}
	printf("人数有%d",sum);
}
*/