#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
//输入小于1000的整数，判断，如果不是小于1000就重输。
/*
int main() {
	int n;
	scanf("%d", &n);
	if (n > 1000 || n < 0) {
		printf("请重新输入小于1000的正数");
		scanf("%d", &n);
		printf("输入的数为：%d", n);
	}else printf("输入的数为：%d", n);
}
*/