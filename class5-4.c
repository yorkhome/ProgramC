#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
//
/*
int main() {
	char c;
	int le = 0, sp = 0, num = 0, oth = 0;
	while ((c=getchar())!='\n')
	{
		if (c <= 'z' && c>='a' || c <= 'Z' && c>='A')
			le++;
		else if (c==' ')
			sp++;
		else if (c <= '9' && c>='0')
			num++;
		else
			oth++;
	}
	printf("le=%d,sp=%d,num=%d,oth=%d", le, sp, num, oth);
}
*/