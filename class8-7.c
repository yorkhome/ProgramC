#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
//换字符串位置
/*
#define m 9
int main() {
	void copystr(char* p1, char* p2);
	char str1[20],str2[20];
	gets(str1);
	if (strlen(str1) < m) {  //判断输入合法性
		printf("输入有误");
	}
	else {
		copystr(str1, str2);
		printf("%s", str2);
	}
}
void copystr(char *p1,char *p2) {
	int n = 0;
	while (n < m - 1) {
		n++; p1++;
	}
	while (*p1 != '\0') {
		*p2 = *p1;
		p1++; p2++;
	}
	*p2 = '\0';
}
*/